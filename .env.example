# =============================================================================
# AMLDO - Configuração de Ambiente
# =============================================================================
# Copie este arquivo para .env e preencha com seus valores reais
# NUNCA commite o arquivo .env no git!

# =============================================================================
# API Keys (OBRIGATÓRIO)
# =============================================================================

# Google API Key para Gemini (obrigatória)
# Obtenha em: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=sua_chave_aqui

# =============================================================================
# Configurações de Modelos
# =============================================================================

# Modelo de embeddings (multilingual para português)
EMBEDDING_MODEL=sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2
EMBEDDING_DIMENSION=384

# Modelo LLM
LLM_MODEL=gemini-2.5-flash
LLM_PROVIDER=google_genai
LLM_TEMPERATURE=0.0

# =============================================================================
# Configurações de Paths
# =============================================================================

# Diretório de dados (relativo à raiz do projeto)
DATA_DIR=data

# Caminho para o índice FAISS
VECTOR_DB_PATH=data/vector_db/v1_faiss_vector_db

# Caminho para CSVs processados
ARTIGOS_0_CSV_PATH=data/processed/v1_artigos_0.csv
PROCESSED_ARTICLES_CSV_PATH=data/processed/v1_processed_articles.csv

# =============================================================================
# Configurações de RAG
# =============================================================================

# Número de documentos a recuperar na busca (recomendado: 12)
SEARCH_K=12

# Tipo de busca: similarity, mmr, similarity_score_threshold
# mmr (Maximal Marginal Relevance) balanceia relevância e diversidade
SEARCH_TYPE=mmr

# Score de diversidade para MMR (0.0 = só relevância, 1.0 = só diversidade)
MMR_DIVERSITY_SCORE=0.3

# =============================================================================
# Configurações de Pipeline
# =============================================================================

# Tamanho dos chunks de texto (em caracteres)
CHUNK_SIZE=1000

# Overlap entre chunks (em caracteres)
CHUNK_OVERLAP=200

# =============================================================================
# Configurações de Ambiente
# =============================================================================

# Ambiente: development, production, testing
ENVIRONMENT=development

# Nível de logging: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Modo debug (mais verboso)
DEBUG=false

# =============================================================================
# Notas
# =============================================================================
#
# - Valores booleanos: true/false (minúsculas)
# - Paths podem ser relativos (à raiz do projeto) ou absolutos
# - GOOGLE_API_KEY é a única variável obrigatória
# - As demais têm valores padrão sensatos definidos em src/amldo/core/config.py
#
